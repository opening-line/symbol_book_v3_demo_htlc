{
    "name": "htlc",
    "private": true,
    "type": "module",
    "workspaces": [
        "packages/*"
    ],
    "scripts": {
        "dev": "nx run-many --target=dev --projects=contracts,frontend --parallel"
    },
    "dependencies": {
        "nx": "^20.0.12"
    }
}
